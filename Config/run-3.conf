# -------------------------------------------------------------------
# Run-3 config: VuFind metadata based schema - TESTING
# -------------------------------------------------------------------

#
# Document subclass
#
document_subclass  = Document::vSolrMetadataAPI::Schema_LS_1

#
# Sizes
#
num_shards_list    = 1
queue_slice_size   = 5

#
# Server key to server URI (1-relative). 
#
# -------------  machine 1  ----------
engine_for_shard_1            = http://dev.solr-sdr:8080/mbooks-3
machine_of_engine_for_shard_1 = http://pilsner.umdl.umich.edu:8080/mbooks-3

#
# shard to index directory map
#
dir_for_shard_1 = /l1/idx/m/mbooks-3/conf/data/index

#
# **** Driver ****
#

#
# host-to-shard(s) map
#
shards_of_host_pilsner = 1

#
# shard(s)-to-host map
#
host_of_shard_1 = pilsner

#
# producer hosts
#
producers_per_host = 4
producer_hosts = assam|bancha|clamato
solr_hosts = pilsner

#
# Max shard size
#
default_max_shard_size_GB   = 1000.0

#
# Error triage - defaults
# 
# Solr could not parse doc
max_indx_errors = 2000;
# Could not create OCR for Solr doc
max_ocr__errors = 1000;
# Could not get metadata for Solr doc
max_meta_errors = 20000;
# Server unavailable
max_serv_errors = 1000;
# Serious stuff
max_crit_errors = 5;
